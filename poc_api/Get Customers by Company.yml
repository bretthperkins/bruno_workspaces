info:
  name: Get Customers by Company
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_endpoint}}/company/Company%20E%20LLC"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
